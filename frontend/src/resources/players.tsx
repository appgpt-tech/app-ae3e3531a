//Source code generated by AppGPT (www.appgpt.tech)
import {
  Datagrid,
  List,
  EditButton,
  Edit,
  SimpleForm,
  Create,
  SelectColumnsButton,
  DatagridConfigurable,
  TopToolbar,
  CreateButton,
  ExportButton,
  FilterButton,
  //Field controls
  BooleanField,
  DateField,
  EmailField,
  ImageField,
  NumberField,
  ReferenceField,
  TextField,
  UrlField,
  //Input controls
  BooleanInput,
  DateInput,
  EmailInput,
  ImageInput,
  NumberInput,
  ReferenceInput,
  TextInput,
  UrlInput,
  PasswordInput,
} from 'react-admin';
import { useRecordContext } from 'react-admin';
import { Grid } from '@mui/material';
const ReadOnlyPasswordField = ({ record, source }) => {
  // You can customize the way you display the password here, e.g., mask it with asterisks
  const maskedPassword = '********';

  return <span>{maskedPassword}</span>;
};
const ListActions = () => (
  <TopToolbar>
    <FilterButton />
    <CreateButton />
    <ExportButton />
    <SelectColumnsButton />
  </TopToolbar>
);
const playersTitle = () => {
  const record = useRecordContext();
  return <span>players {record ? `"${record.playerName}"` : ''}</span>;
};

export const playersList = () => (
  <List actions={<ListActions />} filters={ResourceFilters}>
    <DatagridConfigurable>
      <TextField source="playerName" />
      <NumberField source="currentLevel" />
      <NumberField source="highScore" />
      <TextField source="selectedCharacter" />
      <EditButton />
    </DatagridConfigurable>
  </List>
);

export const playersEdit = () => (
  <Edit title={<playersTitle />}>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <TextInput source="playerName" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="currentLevel" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="highScore" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="selectedCharacter" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Edit>
);

export const playersCreate = () => (
  <Create>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <TextInput source="playerName" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="currentLevel" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="highScore" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="selectedCharacter" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Create>
);

const ResourceFilters = [
  <TextInput source="q" label="Search" alwaysOn />,
  ,
  ,
  ,
  ,
];
